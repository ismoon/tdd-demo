language: java
jdk:
  - openjdk8

branches:
  only:
    - master

#Travis CI 서버의 HOME
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

#script: "./gradlew clean build"
script:
  - echo $AWS_ACCESS_KEY
  - echo $AWS_SECRET_KEY
  - "./gradlew clean bootJar"

#CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - mjis4341@naver.com
